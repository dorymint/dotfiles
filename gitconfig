[user]
	name = yaeshimo
	email = hatsushimoyae@gmail.com

[alias]
	co = checkout
	br = branch
	ci = commit
	st = status
	di = diff
	dr = add --dry-run
	ls = ls-files
	alias = config --get-regexp "alias"
	ls-alias= config --get-regexp "alias"
