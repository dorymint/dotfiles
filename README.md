dotfiles
========

Respect the KISS principle

```sh
# Linux

cd "$HOME"
git clone "$this_repo"

# run setup
"$HOME"/dotfiles/setup/setup.sh

# withx
"$HOME"/dotfiles/setup/setup.sh -withx
  ```

TODO
----

move to config
