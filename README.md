dotfiles
========
hi!


Linux
```sh
cd "$HOME"
git clone "$this_repo"

# run setup
$HOME/dotfiles/setup/setup.sh

# withx
$HOME/dotfiles/setup/setup.sh -withx
```

remember the KISS principle
---------------------------
