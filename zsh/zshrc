# zhsrc

# define DOTFILES_ROOT
source $HOME/dotfiles/share/dotenv.sh

# environment
if [[ -n $DOTFILES_ROOT ]]; then

  # TODO:add any env

  # use grml zshrc
  source $DOTFILES_ROOT/zsh/zshrc.grml
  source $DOTFILES_ROOT/zsh/zshrc.grml.local

  # alias
  source $DOTFILES_ROOT/share/alias
  source $DOTFILES_ROOT/zsh/alias

  # golang
  source $DOTFILES_ROOT/share/goenv.sh

  # function
  function galias() {
    source $DOTFILES_ROOT/share/galias
  }

fi


# setopt
setopt autocd
setopt autopushd
setopt pushdignoredups


# EOF
