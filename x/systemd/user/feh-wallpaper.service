[Unit]
Description=rotate wallpaper use feh

[Service]
Type=oneshot
EnvironmentFile=%h/dotfiles/etc/wallenv
ExecStart=/usr/bin/bash -c '/usr/bin/feh --no-fehbg --bg-max  "${WALLPATH}/$(ls ${WALLPATH} | shuf -n 1)"'

[Install]
WantedBy=
